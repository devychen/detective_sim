1. `extract_{character}.py` to extact all lines, output `{character}_lines.csv`.
2. Do a rough cleaning, code `clean_{character}.py`, output `cleaned_{character}_lines.csv`.
2. Combine all lines into one csv, code `train_lines.py`, output `dev_lines.csv`