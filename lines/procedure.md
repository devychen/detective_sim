1. `extract_{character}.py` to extact all lines, output `{character}_lines.csv`.
2. Do a rough cleaning, code `clean_{character}.py`, output `cleaned_{character}_lines.csv` -> new `clean_lines.py`.
2. Combine all lines into one csv, the train set is then ready. Code `train_lines.py`, output `train_lines.csv`